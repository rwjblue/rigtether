[package]
name = "rigtether-firmware-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Host-neutral RigTether M1 firmware simulator and protocol core"

[dependencies]
rigtether-elecraft-cat = { path = "../elecraft-cat" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
