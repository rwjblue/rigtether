#!/usr/bin/env bash
#MISE description="Validate repository metadata, docs, planning, and protocol vectors"
set -euo pipefail
python3 tools/check_repo.py
python3 tools/check_firmware.py
python3 protocol/conformance.py protocol/vectors/v0.json
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
